#!/bin/bash
# Injects session context at start — current phase and key doc links.
echo '{
  "additional_context": "PKB repo — Phase 1 (source registry and ingestion). Read AGENTS.md and docs/08-ai-development-contract.md before implementing. Use make ci before declaring done."
}'
exit 0
